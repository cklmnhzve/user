<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- LLBTMP_BRANCH 机构表 -->
<mapper namespace="branch">
  <resultMap id="getBranchMap" type="_Branch">
    <id property="id" column="id"/>
	<result property="branchName" column="branch_name"/>
	<result property="branchCode" column="branch_code"/>
	<result property="region" column="region"/>
	<result property="regionEn" column="region_en"/>
  </resultMap>
  
  <select id="getAllBranch" resultMap="getBranchMap">
    select * from llbtmp_branch
  </select>
  
<select id="getBranchName" parameterType="long" resultMap="getBranchMap">
		select b.branch_name from llbtmp_app a , llbtmp_branch b , llbtmp_app_branch c where b.id = c.branch_id and a.id = c.app_id and a.id = #{id}
	</select>
<select id="getBranchId" parameterType="string" resultMap="getBranchMap">
	select id from llbtmp_branch where branch_name = #{branchName}
</select>
</mapper>